<UserControl x:Class="HyperBoostX.Views.PlacementPageChrome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,18,28">
            <Border Style="{StaticResource CyberHeroCardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="270"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontSize="30" FontWeight="Black" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding Subtitle}" FontSize="13" Foreground="{StaticResource Brush.Accent.Primary}" Margin="0,5,0,0" TextWrapping="Wrap" MaxWidth="900"/>
                        <TextBlock Text="{Binding Purpose}" FontSize="13" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,12,0,0" TextWrapping="Wrap" MaxWidth="960"/>

                        <ItemsControl x:Name="HeroPrimaryActionItems" ItemsSource="{Binding PrimaryPlacementActions}" Margin="0,18,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}"
                                            ToolTip="{Binding Tooltip}"
                                            Tag="{Binding}"
                                            Click="RunPlacementAction_Click"
                                            Style="{StaticResource CyberButtonStyle}"
                                            Margin="0,0,10,10"
                                            MinWidth="158"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="22,0,0,0">
                        <Border Style="{StaticResource SuccessBadgeStyle}" Margin="0,0,0,10">
                            <TextBlock Text="Safety Guard Active" Foreground="{StaticResource Brush.Status.Success}" FontWeight="SemiBold"/>
                        </Border>
                        <Border Style="{StaticResource CyberBadgeStyle}" Margin="0,0,0,10">
                            <TextBlock Text="{Binding LastUpdated, StringFormat=Updated: {0}}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                        </Border>
                        <Border Style="{StaticResource CyberBadgeStyle}">
                            <TextBlock Text="{Binding Status, StringFormat=Status: {0}}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.92*"/>
                    <ColumnDefinition Width="1.38*"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CyberCardStyle}" Margin="0,0,14,0">
                    <StackPanel>
                        <TextBlock Text="{Binding StateTitle}" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="{Binding EmptyState}" Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,5,0,14"/>

                        <ItemsControl ItemsSource="{Binding Metrics}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0F1B2B" BorderBrush="{StaticResource Brush.Border.Subtle}" BorderThickness="1" CornerRadius="{StaticResource CornerRadius.Small}" Padding="12" Margin="0,0,10,10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Glyph}" Foreground="{StaticResource Brush.Accent.Primary}" FontSize="10" FontWeight="Black"/>
                                            <TextBlock Text="{Binding Title}" Foreground="{StaticResource Brush.Text.Muted}" FontSize="11" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding Value}" FontSize="20" FontWeight="Black" Margin="0,5,0,2"/>
                                            <TextBlock Text="{Binding Detail}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CyberCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding WorkspaceTitle}" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="Feature evidence, review scope, and recovery proof stay visible before action buttons become the focus." Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                        <ItemsControl ItemsSource="{Binding PlacementSections}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0F1B2B" BorderBrush="{StaticResource Brush.Border.Subtle}" BorderThickness="1" CornerRadius="{StaticResource CornerRadius.Small}" Padding="12" Margin="0,10,10,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource Brush.Accent.Primary}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Description}" Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Style="{StaticResource CyberBadgeStyle}" Margin="0,0,6,6">
                                                            <TextBlock Text="{Binding}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CyberCardStyle}" Margin="0,0,0,16" Visibility="{Binding HasLegacyTools, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Feature Toolkit" FontSize="17" FontWeight="Bold"/>
                            <TextBlock Text="Restored v1.3/v1.4 capabilities are grouped by domain with safety status and route evidence." Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Style="{StaticResource SuccessBadgeStyle}" Margin="12,0,0,0" VerticalAlignment="Top">
                            <TextBlock Text="Domain content" Foreground="{StaticResource Brush.Status.Success}"/>
                        </Border>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding LegacyTools}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#0F1B2B" BorderBrush="{StaticResource Brush.Border.Subtle}" BorderThickness="1" CornerRadius="{StaticResource CornerRadius.Small}" Padding="12" Margin="0,0,10,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel>
                                            <TextBlock Text="{Binding Category}" Foreground="{StaticResource Brush.Accent.Primary}" FontSize="10" FontWeight="Black" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Border Grid.Column="1" Style="{StaticResource CyberBadgeStyle}" Margin="10,0,0,0" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Safety}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                                        </Border>
                                        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Flow}" Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Route}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" Margin="0,8,0,0" FontFamily="Consolas" FontSize="11"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CyberCardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.3*"/>
                        <ColumnDefinition Width="0.82*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="{Binding ActionTitle}" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ActionHint}" Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,4,0,14"/>

                        <ItemsControl ItemsSource="{Binding WorkflowSteps}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0F1B2B" BorderBrush="{StaticResource Brush.Border.Subtle}" BorderThickness="1" CornerRadius="{StaticResource CornerRadius.Small}" Padding="12" Margin="0,0,10,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Step}" Foreground="{StaticResource Brush.Accent.Primary}" FontWeight="Black"/>
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding Detail}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" Margin="0,7,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Evidence Actions" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="Reports, history, help, and restore controls stay grouped here so the main workspace can stay readable." Foreground="{StaticResource Brush.Text.Muted}" TextWrapping="Wrap" Margin="0,4,0,10"/>

                        <TextBlock Text="{Binding SecondaryActionTitle}" FontSize="13" FontWeight="Bold" Margin="0,0,0,8"/>
                        <ItemsControl x:Name="SecondaryActionItems" ItemsSource="{Binding SecondaryPlacementActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}" ToolTip="{Binding Tooltip}" Tag="{Binding}" Click="RunPlacementAction_Click" Style="{StaticResource CyberGhostButtonStyle}" Margin="0,0,10,10" MinWidth="140"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Restore &amp; Rollback" FontSize="13" FontWeight="Bold" Margin="0,8,0,8"/>
                        <ItemsControl x:Name="RestoreActionItems" ItemsSource="{Binding RestorePlacementActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}" ToolTip="{Binding Tooltip}" Tag="{Binding}" Click="RunPlacementAction_Click" Style="{StaticResource CyberGhostButtonStyle}" Margin="0,0,10,10" MinWidth="140"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.18*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CyberCardStyle}" Margin="0,0,14,0">
                    <StackPanel>
                        <TextBlock Text="{Binding ResultTitle}" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ResultSummary}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" Margin="0,10,0,12"/>
                        <Border Style="{StaticResource CyberBadgeStyle}">
                            <TextBlock Text="{Binding Status}" Foreground="{StaticResource Brush.Text.Primary}" TextWrapping="Wrap"/>
                        </Border>

                        <Expander Header="Advanced Details" Foreground="{StaticResource Brush.Text.Primary}" Margin="0,16,0,0" IsExpanded="False">
                            <StackPanel Margin="0,10,0,0">
                                <TextBlock Text="Routes used by this page" Foreground="{StaticResource Brush.Text.Muted}" Margin="0,0,0,6"/>
                                <ItemsControl ItemsSource="{Binding AdvancedRouteLines}" Margin="0,0,0,12">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" Margin="0,0,0,3"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBox Text="{Binding LiveResult}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="170" MaxHeight="340" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Background="#0B1220" Foreground="{StaticResource Brush.Text.Secondary}" BorderBrush="{StaticResource Brush.Border.Subtle}"/>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CyberCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding SafetyTitle}" FontSize="17" FontWeight="Bold"/>
                        <Border Style="{StaticResource WarningBadgeStyle}" Margin="0,10,0,10">
                            <TextBlock Text="{Binding SafetyNote}" Foreground="{StaticResource Brush.Status.Warning}" TextWrapping="Wrap"/>
                        </Border>
                        <TextBlock Text="{Binding RestoreNote}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" Margin="0,0,0,14"/>
                        <ItemsControl ItemsSource="{Binding RestorePlacementActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}" ToolTip="{Binding Tooltip}" Tag="{Binding}" Click="RunPlacementAction_Click" Style="{StaticResource CyberGhostButtonStyle}" Margin="0,0,10,10" MinWidth="150"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CyberCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding RecommendationsTitle}" FontSize="17" FontWeight="Bold"/>
                    <ItemsControl ItemsSource="{Binding Recommendations}" Margin="0,10,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StringFormat=- {0}}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap" Margin="0,0,0,7"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
